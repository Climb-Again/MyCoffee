{
  "name": "mycoffee-api",
  "version": "1.0.0",
  "description": "MyCoffee backend — Fastify service (Node ESM)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "test": "node --test"
  },
  "dependencies": {
    "@fastify/compress": "^8.0.1",
    "@fastify/etag": "^6.0.0",
    "@fastify/helmet": "^13.0.0",
    "@fastify/multipart": "^9.0.1",
    "@fastify/rate-limit": "^10.1.1",
    "fastify": "^5.1.0",
    "google-auth-library": "^10.0.0",
    "js-yaml": "^4.1.0",
    "pg": "^8.13.1",
    "sharp": "^0.35.3"
  }
}
