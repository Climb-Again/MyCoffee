# Backlog — the work list the lane routines read

**This file, not the GitHub issue list, is what a scheduled lane session reads.**
Routine-fired sessions run without MCP connector tools, so they cannot query the
GitHub API. Everything a lane needs to choose its next task must be here.

Each row mirrors a GitHub issue of the same number — the issue holds the full
spec, this table holds the scheduling metadata. Full design detail is in `PLAN.md`.

> **Reconciled 2026-08-27.** This file had diverged between `main` and
> `ios-staging` on **11 of 11 open rows** — `main` showed 4 `ready` + 7 `blocked`
> for work `ios-staging` had already finished, while `ios-staging` still showed
> #65 as `human` after `main` had closed it. Both sides' truth is merged here, and
> `main` is now the single source (see `status/README.md`). Every phase-0→6 row is
> `done`; #91 and #92 are what the audit itself turned up.

**Status values:** `ready` · `blocked` · `claimed` · `done` · `human`
A lane may only pick a row whose status is `ready`, whose lane matches its own,
and **all of whose `needs` are `done`**. Lowest phase first, then lowest number.
After claiming, set the row to `claimed`; after finishing, `done`.

| # | Lane | Phase | Status | Needs | Item |
|---|---|---|---|---|---|







| —  | —         | — | dup     | — | **"search in all text"** (Radu, 2026-08-15) — same as **#56** (listing search doesn't hit full text because the backend search blobs are never populated). Tracked there; no separate row. |












**Note on #75–#77 (filed 2026-08-21 by ios-shell):** PLAN.md §6.8 documents this
feature ("Radu asked for a manual add flow after the shell shipped... supersedes
the earlier 'read-and-review only, no capture UI' stance") and was already
implemented in two commits on `ios-staging` (`f8e227b`/`1acc492`, 2026-08-20,
adding the PLAN section and reversing the design constraint) — but the commit
message's own issue numbers, "backend #36, iOS shell #37, iOS UX #38," collide
with three already-`done` rows that number reused for completely different work
(farm auto-create, per-item review save, roaster countries). No lane could ever
pick this up under those numbers without redoing already-shipped work, and no
status file mentioned "wizard" anywhere before this session — the commit's own
"NEXT: ... must be corrected" follow-up was never done. Filed fresh, unused
numbers instead (`#75`/`#76`/`#77`) and corrected PLAN.md §6.8's own
cross-reference to match, same "correcting a task means correcting this file"
precedent as every `#NN→#MM` decomposition above. No code changed by this
correction — `#75` (backend) is the only row actually `ready`; nothing else
unblocks yet.






> **⚠️ Value-meter conflict (2026-09-02):** `UPDATE_BRIEF.md`'s "Value meter logic" section restates v2's **price-band** logic and says keep it — but Radu's LATEST word (this same day, → **#109**) is **cheap-for-quality** (`ratingPct × cheapPct`, library-wide). **#109 wins over the brief's value section** (the brief's value text predates that decision). A lane must NOT revert #109 to price-band logic on account of the brief. **Radu 2026-09-02: KEEP the 5-band scheme as agreed in #109 — OVERPAID / POOR / FAIR / GOOD / GREAT (5 pills). The brief's "drop POOR, use 3 labels" reduction is OVERRIDDEN; build #109 exactly as filed.**





| 126 | backend | 8 | ready | 166 | **UNBLOCKED 2026-09-10: #166 is done and the OCR blocks are restored.** Re-measure what (if anything) a captioned coffee is still missing from its bag photo before doing more — much of the original symptom was #166's wipe. **RE-BLOCKED 2026-09-09 (audit) on #166: the "ZERO coffees have an OCR text block" measurement this row is built on is the symptom of #166 — re-adjudication wipes appended OCR text. Fix #166, re-run the OCR backfill once, then re-measure; whatever is still missing after that is this row's real scope (likely much smaller).** **The bag photo is never OCR'd for any coffee that has caption text — so everything printed only on the bag is lost.** (Radu, 2026-09-06, screenshot of the WatchHouse *Nguvu Washed* card: "this has no ocr and there's all needed data in the photo".) Live check of `UaVLSUF9Fb7MxiOi67DA8g`: the photo plainly shows **Nguvu Washed / Burundi / Buhumuza Province / Muyinga washing station / Washed / 1717 MASL / Bourbon / Caramel|Honey|Apricot**, and the record has `originCountryIds: []`, `altitudeMidM: null`, `originFarmId: null`, `descFarmLot: null`, `minFieldConfidence 0.00` — the app therefore renders it as **"Unlabeled coffee"** with no flag. Its `rawDescription` is *only* the pasted blurb (the 3.9/5 quote), which contains none of those fields. **Root cause — a rule, not a one-off:** `shouldUseImage(photo, includeImages)` (`worker.js:84`) returns true only when the job asks for images **or** the photo is `awaiting_text`. A photo that arrives with any caption text lands in `text_received`, and §10's merged ingest routine runs a **text-only pass first** and escalates to images only if *image-only* photos remain — which they never do. So a captioned photo is extracted from its caption **forever** and its image is never sent to a voter. The OCR transcription append is gated the same way (`if (includeImages && image && result?.coffeeId)`, `worker.js:764`), so it never fires either. **Measured across all 413 live coffees: ZERO have an `OCR text` block.** The #67/#79 feature has effectively never run in production. **Damage, measured, not guessed:** 72 coffees with no farm/lot name at all, 68 at `minFieldConfidence 0.00`, 199 with no weight, 234 with text but no price (which is exactly what starves #106/#125's value half), 116 with no altitude, 50 with no flavour notes, 5 with no origin country. **⚠ CORRECTION to this row's first write-up (Radu, 2026-09-06: "but actually some HAVE ocr") — he is right and my framing was wrong.** "Zero coffees carry an `OCR text` block" is literally true (I re-checked every heading variant, and `appendOcrTextToCoffee` always writes the same one), but I wrote it up as "everything printed on the bag is lost", which it is not. **226 of 413 records already contain the full bag card verbatim** — e.g. the sibling `9SDz09wxLSRIU-C-B2mhMA` (*Nguvu Natural*) has `Origin. Burundi / Region. Buhumuza Province / Process. Natural / Altitude. 1717MaSL / Varietal. Bourbon / Notes. Mango | Strawberry | Brown Sugar` — measured as ≥3 structured field markers in `raw_description`. That text is **Radu's own, supplied at ingest** (every single `raw_caption` is null; `photo_texts.description` is what the osxphotos ingest writes), not anything an agent read. So the mechanism above is real and unchanged — the image is never sent for a captioned photo — but the *damage* is confined to the **187 thin/blurb records**, of which 23 sit at `minFieldConfidence 0.00`. Split by that line, the nulls are: bag-like 226 → 114 no price, 118 no weight, 49 no farm/lot, 28 no altitude; thin 187 → 135 no price, 81 no weight, 84 no farm/lot, 88 no altitude. Prices and weights are printed on bags far more often than they appear in a pasted blurb, so this is still a large recoverable gap — just not the whole library. **Two levers, and they are NOT interchangeable — both are needed:** (1) `POST /api/admin/backfill-ocr-text {"includeCaptioned": true, "limit": N, "spendCapUsd": X}` already exists and is idempotent (`NOT LIKE '%OCR text%'`), and recovers the **text** — it also nulls a `''` flavour-notes sentinel so #80's pass re-reads the richer text. But it only calls `runOcrTranscribe`; it **never re-runs the voters**, so no structured column moves. (2) Recovering `origin/region/altitude/varietal/farm-lot/price/weight` needs a **re-extraction with images**. `POST /api/admin/adjudicate` cannot do it — same limitation #124 documents: it only re-canonicalizes *already-stored* `field_candidates`. Good news: `computeInputSha` includes `imageSha`, so a vision run is never confused with the cached text-only one and will genuinely re-run (i.e. it costs money — bound it with `spendCapUsd` and stage it over days against the quota, as #67 did). **Decisions for Radu before the lane spends anything:** (a) run the cheap OCR-text backfill over all 413 first and see how much it recovers on its own — recommended, it is bounded and reversible-ish; (b) then decide the budget for an images-on re-extraction, and whether to scope it to the worst rows (the 68 at 0.00 confidence + the 72 nameless) rather than all 413; (c) **the standing rule change**: a captioned photo should get **one** image pass, not zero — e.g. escalate `text_received` to images when the text-only pass leaves core fields unresolved, so this cannot silently re-accrue on every new bag. `backend/src/lib/worker.js`, `backend/src/routes/admin.js` (+ tests). Relates to #124 (same "re-adjudication can't reach it" shape), unblocks the value half of #106/#125. |
| 127 | backend | 8 | ready | 126 | **Extract the coffee's own product name off the package into a new `product_name` field.** (Radu, 2026-09-06: "all coffees have a prominent name on the package. I want that to become the default name in app instead of farm name… for example 'The Alchemist', 'Wild Watermelon Experience', 'Strawberry cheesecake'".) **No such field exists today** — not in `coffees`, not in the voter prompts, not in `adjudicate.js`'s canonicalizers. What the app currently titles a coffee with is `rawTitle`, i.e. **`photo_texts.title`, which is Radu's own Photos-library title**, not anything read off the bag: 331 of 413 have one and they read like `Cafea The Barn Sitio Cachoeirinha (Sao Paolo, Brazil) IUNIE 2020` or `DAK Lulo Bingo - Colombia`. The wanted name (`Lulo Bingo`, `Saint-Tropez`, `Cinnamon Roll`, `Sweet Valley`) is *inside* those strings but never isolated, so it can't be surfaced without extracting it. **Work:** migration adding `coffees.product_name` (nullable text); a `product_name` key in the extraction prompt + `canonicalize()` (trim, strip a leading roaster name and a trailing origin/process/date tail, cap length, reject a value that is merely the roaster or the origin country); expose it on `/api/coffees` and the detail payload; and a bounded admin backfill over existing rows that reads each coffee's assembled text — **which is exactly why this needs #126 first**: the OCR block appended there is where a bag-printed name lives for the coffees whose pasted text is a thin blurb. Respect the locked/`decided_by='human'` invariant so a human edit is never overwritten, and make it editable through the generic edit endpoint like any other field. `backend/migrations/`, `src/lib/{prompts,adjudicate}.js`, `src/routes/{coffees,admin}.js` (+ tests). |
| 128 | ios-ux | 9 | blocked | 127 | **Make the package's product name the default coffee title.** (Radu, 2026-09-06 — same message as #127.) `Coffee.displayTitle(vocabulary:)` (`Features/Coffees/CoffeeDisplay.swift:11`) currently resolves `rawTitle` → farm name → origin country → `"Unlabeled coffee"`. New precedence: **`productName` → `rawTitle` → farm → country → "Unlabeled coffee"** — Radu's instruction is explicitly "when not detected use what's currently on", so the existing chain stays intact underneath rather than being replaced. One-line-ish change in the UX lane once #127 ships the field through `Coffee`; the shell lane owns adding `productName` to the model + snapshot decoding, so **this row needs a claim in both iOS lanes** (§4's shell/UX seam). Check every call site of `displayTitle` — rows, detail header, rails, share sheet, search blob — so the new name is used consistently and search still matches on it. |
| 132 | backend | 9 | done | — | **Roaster pages: store a logo + surface the (already-existing) blurb through the snapshot.** (Radu, 2026-09-06: "add content to roaster pages — logo + text… I will name files with roaster name; for text I'll paste blurbs for each roaster.") The `roasters` table **already has a `blurb TEXT` column** (`004_vocab.sql:31-47`) — it is simply never populated and never read out: `loadRoasterVocab()` (`src/lib/vocab.js:141`) selects only `id, name, slug, country_id`, and the `/api/snapshot` vocab block (`loadVocabDictionary`/`toCompact*` in `src/routes/coffees.js:133`) carries only those. There is **no logo column**. **Work — spans backend + data, needs a claim in BOTH lanes (§4 seam):** (backend) new migration adding `roasters.logo_url TEXT` nullable (next number `029_*`), and extend the snapshot vocab shape in `coffees.js` to include `blurb` + `logo_url`; (data) extend the `loadRoasterVocab` SELECT in `vocab.js` (Data-owned) to return `blurb, logo_url`, and populate values from Radu-supplied content (see #133). **Logo hosting is an open decision for Radu — DON'T pick one silently:** the existing image pipeline (`routes/media.js`, `assets` table) is photo-asset-keyed and a logo has no photo; the low-risk path is a remote/CDN `logo_url` fetched straight through the existing `ImageStore`, but logos then compete with coffee thumbnails for the **30 MB cache / 50 MB app cap** (CLAUDE.md §12) — ask before exceeding. `backend/migrations/**`, `src/routes/coffees.js`; `src/lib/vocab.js` (data). |
| 152 | backend | 9 | ready | 132 | **Roaster content WRITE path — let the app submit a blurb + logo for a roaster (enables the in-app "missing content" flow, #153).** (Radu, 2026-09-07: "notify me in the app to upload text + logo for any future roaster that has none — ideally it happens in the app in case I don't upload here.") The read path is done (#132/#133/#134: `loadRoasterVocab` selects `blurb, logo_url`, snapshot carries both, the Swift `Roaster` decodes them), but there is **no write endpoint** — roaster content today only enters via the `ops/roaster-assets/` chat/GitHub flow + the one-time #133 backfill. Add an authenticated endpoint to set a roaster's blurb and store a logo, keyed on `slug` (the stable key), e.g. `PATCH /api/roasters/:slug` for the blurb text and a logo upload (multipart or base64) that persists the image and sets `logo_url`; **require the ingest/write token** (same auth as the wizard write endpoints — the app already holds it in the Keychain). **Two decisions for Radu — DON'T pick silently:** (1) **logo hosting/storage** — this is the still-open #132 decision, now forced: the existing image pipeline (`routes/media.js`, `assets` table) is photo-asset-keyed and a logo has no photo, so either extend it to hold non-photo assets, add a small logo blob store, or accept a remote/CDN URL; whichever it is, uploaded logos then compete with coffee thumbnails for the **30 MB cache / 50 MB app cap** (CLAUDE.md §12) — server-side downscale to the same ≤512px WebP the `ops/normalize-logos.py` intake already produces. (2) whether an in-app write becomes **source of truth** over the `ops/` folder for that roaster (recommend yes — once a roaster is edited in-app, the DB wins and the checklist should read DB truth, not the folder). `backend/migrations/**` (if a store/columns are needed), `backend/src/routes/**`. |
| 153 | ios-ux | 9 | blocked | 152 | **In-app nudge + editor to add a missing roaster logo/blurb (the feature Radu asked for).** (Radu, 2026-09-07, same message.) Because the snapshot already carries `blurb`/`logoUrl` (#134 done), the app can already tell which roasters lack content — no backend read work needed to *detect* it. Build: **(a) detection + nudge** — surface roasters in the library whose `blurb == nil/empty` OR `logoUrl == nil` (a count/badge + a "N roasters need content" row, e.g. on Insights/roaster list and/or the roaster page itself showing a "help complete this page" prompt when its own content is missing); **(b) in-app editor** — a sheet to type/paste the blurb and pick a logo image (PhotosPicker), submitting via the #152 write endpoint, then folding the returned `blurb`/`logoUrl` into the in-memory `Roaster`/vocab so the page updates without waiting for a full resync. **⚠ Shell seam:** the write call needs a client API surface (DTO + `APIClient`/`SyncEngine`/`CoffeeRepository` method + a `CoffeeStore` passthrough, mirroring how #136 added `evaluate`) and a store-side "roasters missing content" derivation — both `ios-shell`-owned — so **claim in BOTH `ios-ux` and `ios-shell`.** **Decision for Radu — DON'T pick silently:** "notify me in the app" scope — a passive in-app banner/badge/section (what he literally asked for, recommend shipping first) vs a real iOS local notification/app-badge nudging him when a new roaster arrives with no content (bigger: needs a BGTask/notification permission — file as a follow-up if he wants it). Respect the size cap — the picked logo is uploaded, not cached full-res. `Sources/Features/**` (ux), `Sources/{Store,API,Models}/**` (shell API surface). Depends on #152. |
| 154 | ios-shell | 9 | blocked | 152, 153 | **Push notification when a new roaster lands with no logo/blurb (Radu wants a real push, not just the in-app nudge).** (Radu, 2026-09-07: "add push notification as well.") Extends #153's in-app nudge with an APNs push so Radu is pinged even when the app is closed. **Greenfield — no push/APNs infra exists anywhere** (repo-wide grep for `apns`/`aps-environment`/`UNUserNotification`/`deviceToken` is empty). Spans three lanes + one manual Apple step, so it needs claims accordingly: **(ios-shell/publish)** add the Push Notifications capability + `aps-environment` entitlement (`ios/project.yml`, entitlements file) — note the **`match` App Store profile on `mycoffee-private` must be regenerated to include Push**, and `UIApplication` remote-registration returning the APNs device token; **(ios-shell)** register for `UNUserNotification` authorization at a sensible moment (not cold launch) and POST the device token to the backend; tapping the push deep-links to #153's in-app editor for that roaster. **(backend)** a device-token store, an APNs sender (JWT `.p8` auth-key based), and the trigger: when ingestion resolves/creates a roaster that has no blurb AND/OR no `logo_url`, enqueue a push. **Manual step owed by Radu (like the App ID work):** create an **APNs Auth Key (.p8)** in the Apple Developer portal under team `PH2NNQ47UB` and add it as a GH/Railway secret — `produce`/CI can't mint it. **Decisions for Radu — DON'T pick silently:** (1) trigger cadence — one push per new empty roaster (risk: spammy on a big ingest) vs a **throttled daily digest** ("3 roasters need content") — recommend the digest; (2) whether push also covers roasters missing *only* a logo vs *only* a blurb vs either. File the manual APNs-key todo in `BUILD_STATUS.md` when this is picked up. `ios/project.yml` + entitlements + `Sources/App|Store/**` (shell), `.github/workflows/**` + `match` (publish, private repo), `backend/src/routes|lib/**` + `migrations/**` (backend). Depends on #152 (write path to resolve the content) and #153 (the editor the push lands on). |
| 155 | backend | 7 | done | — | **DONE 2026-09-10 (interactive — Radu: "implement 155").** Shipped `main@0aca651` (Railway run #97 green). Migration 035 (52 seed options live: 10 device / 21 grind / 16 temp / 5 recipe), `lib/brewState.js`, `routes/brew.js`, snapshot `vocab.brewOptions` + compact-row `brewTried`/`brewBest`. 437 tests. Live end-to-end: `POST /api/coffees/:id/brew` set the 4:6 recipe = best → `brewBest`=[recipe], `brewTried` auto-ticked 28 clicks + 92 °C; cleaned up after. The backend surface #156 waits on is now live. **[Re-phased 7←10 on 2026-09-10 — Radu: build the Brew lab right after the OCR-data-loss fix #166. #156–#158 moved to phase 7 with it.]** **Brew lab — data model + API + snapshot (PLAN.md §14).** (Radu, 2026-09-09: "recipe catalogue and checkboxes per coffee plus rate per recipe per coffee (just check 'best recipe'); brew devices catalogue and checklist per coffee plus rate (best device); grind size and temp tested per coffee with rating per pair (just check 'best grind size' / 'best temp') … check what recipes/devices/grind sizes/temps I used for each coffee and pick the winner per coffee.") **Radu, 2026-09-09 follow-up: grind = Comandante clicks; recipe = structured (coffee g, number of pours, ml/pour, total water, grind clicks, water temp).** **Greenfield — nothing brew-related exists** (repo-wide grep: only `desc_brew_guide` prose). Ship: **(a)** migration `034_brew_options.sql` — one catalogue table `brew_options (id, kind ∈ recipe/device/grind/temp, label, label_norm UNIQUE per kind, detail, value_num, sort_order, archived_at, …)` **+ six recipe columns `dose_g, pours, ml_per_pour (nullable — uneven pours), total_water_ml, grind_clicks, water_temp_c`** (CHECK: NULL on non-recipe rows, complete + in-range on recipe rows; `value_num` 1–60 for grind, 60–100 for temp, NULL otherwise; **unique `(kind, value_num)` for grind/temp**) + join table `coffee_brew_trials (coffee_id, option_id, kind, is_best, tried_at; PK (coffee_id, option_id))` with the **partial unique index `(coffee_id, kind) WHERE is_best`** so one-winner-per-kind is DB-enforced, plus the idempotent seed in §14 (10 devices, **21 grind rows = Comandante clicks 16–36**, 16 temps 85–100 °C, **5 structured recipes** — V60 1-cup, 4:6, AeroPress, Chemex, French press — whose clicks/temps all exist as grind/temp rows); **(b)** `routes/brew.js`: `GET /api/brew-options`, `POST /api/brew-options` (`{kind, label, detail?, valueNum?, recipe?: {doseG, pours, mlPerPour?, totalWaterMl, grindClicks, waterTempC}}`; get-or-create on duplicate `label_norm` or duplicate `(kind, valueNum)`, 201/200; grind/temp labels are **server-generated** from the value — "24 clicks" / "94 °C"; recipe must be complete (400 `invalid_recipe`) and `pours × mlPerPour` within ±5 % of `totalWaterMl` when given (400 `recipe_water_mismatch`)), `PATCH /api/brew-options/:id` (label/detail/valueNum/recipe/sortOrder/archived — archive, never delete); **(c)** `POST /api/coffees/:publicId/brew {optionId, state: untried|tried|best}` — human-only write mirroring `/favorite` + `/rotation` (no `resolveField`), one transaction: `best` demotes the previous best of that kind, **a recipe set to tried/best also upserts `tried` rows for the grind row with `value_num = grind_clicks` and the temp row with `value_num = water_temp_c` (get-or-create; one-way — un-trying the recipe leaves them)**, always bumps `coffees.updated_at`, responds with the coffee's whole `{brewTried, brewBest}`; **(d)** snapshot: `vocab.brewOptions` (full, every sync) + compact-row `brewTried`/`brewBest` int arrays **omitted when empty**, via one `LEFT JOIN LATERAL array_agg` — **no `SNAPSHOT_VERSION` bump** (purely additive; no-trial coffees correctly read as none); same two fields on `GET /api/coffees/:publicId`. Tests `test/brew.test.js` (no-DB validation incl. recipe completeness/water mismatch, `rotation.test.js` shape) + pure `lib/brewState.js` helpers unit-tested: `nextTrialRows` ("best demotes old best") and `impliedTrials(recipe)` (exactly one grind + one temp tried row). Check `GET /api/admin/jobs` before pushing `backend/**`. Verify live: `GET /api/brew-options` → **52** seed rows; `POST …/brew` marking 4:6 `best` on a real coffee → `brewBest` has the recipe and `brewTried` also has 28 clicks + 92 °C in `/api/snapshot?since=`. Full spec: PLAN.md §14. |
| 157 | ios-ux | 7 | done | 156 | **DONE 2026-09-11 — `ios-staging@1be0dae`, compile-green at run #114.** Coffee page: a **BREW LAB** block between the fact rows and the notes — empty shows "Log what you brewed with →", with trials a 2×2 grid (one cell per kind) showing the winner's label with a trophy, else "n tried", else an em dash; hidden entirely while `reviewState == "unextracted"`. `Features/Brew/BrewLabSheet.swift`: recipe/device as rows (leading checkbox = tried, trailing trophy = best, recipes showing `BrewRecipeSpec.summary`; unticking the current winner asks first — it is the one destructive tap on a checklist), grind/temp as `WrapLayout` chip grids (tap = tried, long-press = best with a haptic) because 21 Comandante clicks and ~16 temperatures as rows would be a 37-row scroll to find one number. Archived options hidden unless this coffee tried them. Per-section "Add <kind>…": device inline field, grind/temp wheels (1–60, 60–100 °C), recipe a full `RecipeFormSheet` (live "Ratio 1:15.0" footer; total water prefilled from pours × ml/pour and syncing until typed over). New options arrive already ticked. `Features/Brew/BrewCatalogueView.swift` from Settings: segmented by kind, swipe edit/archive, drag-reorder recipes and devices, per-row "won 7 of 12", **no delete** (options are referenced by every coffee that tried them) and **no rename for grind/temp** (there the value IS the identity — renaming "24" would silently change what every coffee using it means). **DEVIATION:** the row asked for a Lucide `trophy` SVG; the Lucide set is vendored from files Radu supplied and inventing a lookalike would put a foreign glyph in it, so `Symbols.trophy`/`trophyFill` are SF Symbols — swapping in his SVG is a one-line change. **UNBLOCKED 2026-09-11: #156 shipped `ios-staging@9aa21a1`.** Brew lab — coffee-page section + `BrewLabSheet` + Settings catalogue (the feature Radu asked for; PLAN.md §14).** On `CoffeeDetailView.card` between `priceBlock`/`factRows` and `notesSection`: a **BREW LAB** section-label (the #145 10pt/0.12em treatment). Empty → one line "Log what you brewed with →"; with trials → 2×2 grid (#145's 12pt/18pt gaps), one cell per kind (recipe/device/grind/temp), value = **winner's label** semibold + small Lucide `trophy` (add SVG to `Resources` + `Symbols`), else "n tried" neutral-700, else "—". Tap → `BrewLabSheet` (`.large` detent): four sections in order recipe → device → grind → temp. **Recipe + device = list rows** `[✓] label … 🏆` — leading checkbox = tried (toggles untried↔tried; unticking a winner asks via `confirmationDialog`), trailing trophy = best (fills, implies tried, the previous winner in that section animates off — never two trophies); label weight semibold/regular/neutral-700 by state; recipe rows show `BrewRecipeSpec.summary` as the one-line secondary ("20 g · 5 × 60 ml · 300 ml · 1:15 · 28 clicks · 92 °C"), free `detail` as a third line. **Grind + temp = chip grids** (`WrapLayout`; 21 Comandante clicks and 16 temps as rows would be a 37-row scroll): header "Grind · Comandante clicks" / "Temperature", chip "24" / "94°", **tap = tried, long-press = best** (haptic, trophy badge), outline/neutral-fill/accent-fill by state. **Ticking a recipe also ticks its grind/temp chips (server rule, PLAN §14) — animate those chips so the cause is visible.** Archived options hidden unless this coffee tried them (then greyed + "archived" tag). Last row per section **"Add <kind>…"**: device → inline TextField; grind → **wheel Picker 1–60 clicks**; temp → **wheel Picker 60–100 °C**; recipe → **`RecipeFormSheet`** (name; coffee g decimal; pours Stepper 1–12; ml/pour optional int; total water int prefilled `pours × ml/pour` until overridden; grind wheel 1–60 clicks; water temp wheel 60–100 °C; optional note; live "1:16.7" ratio footer; Save disabled until complete). New option appears **already ticked as tried**; duplicates resolve to the existing option. Section headers "Device · 3 tried". All taps optimistic; `brewErrorText` as a non-blocking toast (the `editErrorText` pattern). **Settings → "Brew catalogue"** (`BrewCatalogueView`, segmented by kind): swipe **Edit** (device: rename alert; recipe: `RecipeFormSheet` pre-filled → `updateBrewOption`; grind/temp: no edit — the value is the identity, archive + add instead) / Archive-Unarchive, drag-reorder recipes/devices → `sortOrder` (grind/temp always by value), **no delete**. Hide the section on `reviewState == "unextracted"` placeholders. Rules: three shadows only (#146), no capsules, 44pt hit areas, omit-not-"N/A". **Acceptance:** glance shows the four winners or "nothing logged"; two taps mark a device tried+best and the old winner loses its trophy; kill offline mid-tap → relaunch keeps the ticks and the next sync doesn't undo them; "Orea V3" added from one coffee is in every coffee's list; marking "4:6" tried on a coffee shows 28 clicks and 92 °C ticked too. Sync the row flip to `main`. |
| 163 | human | 9 | human | — | **Nothing here blocks the extension any more — both open questions were given working defaults on 2026-09-09. This row exists so Radu can override them, not so he must answer them.** **(1) #106's ship gate — resolved by design, gate still honoured.** #106 forbids shipping the headline number until Radu has seen real evaluations and agrees they read sensibly. Rather than block #125 and #162 on that, **build both with the headline behind a flag**: the components (affinity / value / novelty) and the `low`-confidence suppression were never gated, only the single number was. He then sees real evaluations from his own browsing — far better evidence than the 10 synthetic LOO samples — and flips the flag with one word. **Nothing to do now.** For reference, the existing sample (`status/backend.md`, 2026-08-31) is directionally sensible (the two highest actual ratings, 4.8 and 4.9, got the two highest headlines) but noisy per bag (a 4.1 scored 52); that is what r≈0.39 with 58% of the corpus rated exactly 4.0 predicts, not a bug. **5 of 10 produced no headline for want of a price**, which shop pages should largely fix. **(2) Who builds `extension/**` — decided: the backend lane.** It already owns the API the extension consumes and it actually fires (Mon+Thu), whereas lane `ext` matched no cron and would have stranded #162 indefinitely. #162 is relabelled and CLAUDE.md §4 updated. **Override either of these by saying so — that is the only action this row asks for.** ⏱ **One thing worth knowing:** the backend lane has five phase-8 rows (#121, #124, #126, #127, #129) queued ahead of #160/#161, so on cadence alone the extension starts in roughly three to four weeks. Say the word to pull it forward. |
| 166 | backend | 6 | done | — | **DONE 2026-09-10 (interactive — Radu: "implement 166").** Code shipped `main@83f756f` (Railway run #96 green): `mergeRawDescription` preserves the OCR block on re-derive, `refreshSearchBlobs` now runs after the append (live path + backfill); 422 tests. OCR blocks restored via a bounded backfill (all restorable rows; coffee id `7` stays illegible — that is #129, not this row); `rebuild-search-blobs` updated 414 rows so restored OCR is searchable. Live-verified. **[Phase 6 on 2026-09-10 so it stays the single first backend pick, ahead of the re-prioritized Brew lab #155.]** **Re-adjudication WIPES the appended OCR text block — and OCR text is never searchable. Fix both, then re-run the OCR backfill once. (Audit 2026-09-09; pick this before every other backend row.)** `adjudicateAndApply` (`backend/src/lib/worker.js:694-697`) does `UPDATE coffees SET raw_title=$1, raw_caption=$2, raw_description=$3` straight from `photo_texts`, discarding whatever `appendOcrTextToCoffee` (`:802-817`) appended earlier. Fine inside `processPhoto` (`:758`, append happens after at `:764`), **destructive** from `readjudicateAll` (`:1102`) and `backfillRoastDates` (`:1175`). Timeline proves it: #65 "every image-bearing coffee has its OCR block" (08-26) → #91 `POST /api/admin/adjudicate` over 411 photos (08-28) → #126 "ZERO coffees have an OCR text block" (09-06). Side effects: the `flavor_notes=''` sentinel is not reset, and `updated_at=now()` on 411 rows forces a full delta resync on every device. Also `refreshSearchBlobs` runs inside `adjudicateAndApply` (`:758`) *before* the append, and the append never refreshes blobs — so `search_prose_blob`/`search_tsv` never contain OCR text even when it exists. **Fix:** (a) only rewrite `raw_*` when the `photo_texts` row actually changed, and always preserve an existing `\nOCR text\n…` tail of `raw_description`; (b) call `refreshSearchBlobs(coffeeId, sharedCtx)` after a successful append; (c) don't bump `updated_at` when nothing changed; (d) unit-test (a) with a pure helper (`mergeRawDescription(existing, incoming)`); (e) then run `POST /api/admin/backfill-ocr-text` **once, bounded** (check `GET /api/admin/jobs` idle first) and confirm the block count is back. Re-points #126 (now `needs 166`). |
| 178 | ios-shell | 9 | done | — | **DONE 2026-09-14 (ios-shell), `ios-staging` — compile-green run #134.** **(a)** `Coffee`'s three copiers each spelled out all 35 initialiser arguments; properties are now `private(set) var` and `withFavorite`/`withRotation`/`withBrew` all go through one private `with { $0.x = y }`. Still immutable outside the file. (The old doc comment justified all-`let` as what made `Coffee` `Sendable` — that was never the reason; a struct is `Sendable` when its stored properties are.) **(c)** `refresh()`/`loadDetail()` no longer swallow errors into `try?`: new `@Published lastSyncError`/`lastSyncedAt`, with the index deliberately left untouched on failure so a bad sync can never blank the list. **(b)** `CompactCoffeeDTO.purchasedOn` is optional now — it was the one required DTO date the DB allows NULL, so such a row threw and `FailableDecodable` dropped it silently. `makeCoffee` is failable and the drop is **counted** (`SnapshotDecodeStats` → `CoffeeStore.droppedRowCount`, shown in Settings by #192). **`Coffee.purchasedOn` itself is deliberately NOT widened** — it feeds the canonical sort order, the year facet and three Insights aggregations across ~15 UX-owned call sites, so it is a cross-lane change and gets its own row rather than being half-done here; 0 of 414 live rows are affected today (measured), and the wizard's `quick-create` path is how it would start. **(e)** `CoffeeStore.makeAPIClient()` is the one shell entry point; the two shell-owned ad-hoc `APIClient(config:)` sites are migrated, the six UX ones are an additive follow-up (seam rule). **(d) NOT done, and this row's own claim is partly wrong:** `extractWizardDraft`/`createWizardCoffee` are **not** callerless — they are the `CoffeeStore` end of a `CoffeeRepository` protocol requirement implemented by both `RemoteCoffeeRepository` and `SampleCoffeeRepository`, so removing them means editing the protocol and both conformances. And `SampleData`/`SampleCoffeeRepository` are referenced only from doc comments, but `SampleData` was edited two commits earlier (#203, `ios-staging@41d5180`) to give the DAK sample roaster a live `logoUrl` "so the tile has something to render in sample mode" — someone believes sample mode is reachable, and deleting on the strength of a grep would be guessing. — Original spec follows. **Shell hygiene batch (Audit 2026-09-09).** (a) `Coffee` has six 35-argument initialiser call sites (`Coffee.swift:122-135, 143-158, 163-178`, `CoffeeMapping.swift:23-63, 71-109`, `SampleData.swift:190-260`); the doc comment's Sendable argument for all-`let` is wrong — `private(set) var` + one `func with(_ mutate: (inout Coffee) -> Void) -> Coffee` keeps immutability and deletes both copiers (and simplifies #156's `withBrew`). (b) `purchasedOn` is the one required DTO field the DB allows NULL (`SnapshotWire.swift:106` vs `008_coffees.sql:26`); `FailableDecodable` (`FlexibleDecoding.swift:13`) drops rows silently — make it optional and count dropped rows into a debug field shown in Settings. (c) Swallowed errors: `CoffeeStore.swift:62` (`try? refresh()` — pull-to-refresh failure invisible), `:86` (`try? loadDetail`), `SyncEngine.swift:74` (`try? snapshotText()`); add `@Published lastSyncError`/`lastSyncedAt` for the UX to surface (#180). (d) Dead code: wizard confirm path superseded by #118/#131 — `extractWizardDraft`/`createWizardCoffee` (`CoffeeStore.swift:218-240`, `CoffeeRepository.swift:65,76`, `Models/CoffeeDraft.swift`, `API/Wire/CoffeeDraftWire.swift`); `MutationOutbox.swift:56-66, 79-120` enqueue/pending helpers with no callers; `SampleData.swift` + `SampleCoffeeRepository.swift` referenced nowhere and drifted (`roasterId: Int` vs `Int?`). **Keep** the `evaluateCoffee` chain — #125 consumes it. (e) `loadBrief`/`refreshReviewCount` (`CoffeeStore.swift:260-271`), `ReviewFeedCache.swift:55-56`, `SettingsSheet.swift:52`, `WhatsNewView.swift:64` each build their own `APIClient` outside the repository — expose one shell entry point. |
| 192 | ios-ux | 8 | done | 178 | **DONE 2026-09-14 (ios-ux), `ios-staging`.** #178's fields are surfaced two ways, because they answer two different questions. A **"Last sync" section in `SettingsSheet`** — deliberately separate from the existing "Backend" section, which answers *can I reach it right now*, while this answers *did the last sync actually work*, previously unanswerable — showing `lastSyncError`, `lastSyncedAt` and #178(b)'s `droppedRowCount`. Rows-dropped renders **even at 0**: a diagnostic that only appears when non-zero is one nobody learns to trust. And a **tappable banner on the Coffees list** (same visual language as the review nudge, not an error screen), because the list keeps showing the last good data on a failed refresh — so the banner is the only thing distinguishing "nothing changed" from "we couldn't ask". Tapping retries. — Original spec follows. **Split from #179(f) (2026-09-11) — #179's (a)-(e) shipped `ios-staging@14f6257`, this is just the part gated on the shell lane.** Surface #178's `lastSyncError`/`lastSyncedAt` (`CoffeeStore.swift`, once #178 adds them) in `SettingsSheet` + a small banner, same toast/banner treatment as `editErrorText`/`brewErrorText`. Blocked until #178 ships those fields — `ios-shell`'s own row, still `ready` as of this row's filing. |
| 182 | publish | 9 | done | — | **DONE 2026-09-14.** `status/BACKLOG.md` went from **435 KB to 58 KB** (197 rows → 21 live). **(a)** every `done`/`dropped` row that no open row still `needs` moved verbatim to `status/archive/BACKLOG-done.md` — reading this row's two criteria as it writes them ("phase < the lowest open phase, **and** any `done` row no open row `needs`"), because the phase clause alone left 243 KB: the lowest open phase is 8, so every phase-8/9/10 `done` row would have stayed, and those are exactly the ones nothing depends on any more. A row an open row needs stays in the live file, so a lane checking its dependencies never has to open two. **(b)** `check-backlog.sh` reads **both** files for the duplicate check and for `needs` resolution (verified: injecting an archived row number into the live file fails the check across files, and a `needs` pointing at an archived row passes). The archive is optional, so a checkout predating the split still passes. **(c)** the ~52 KB of 2026-08 prose under the table moved to `status/archive/BACKLOG-notes-2026-08.md` with a pointer left behind. **(d)** `status/README.md`'s numbering rule now greps both files — max over the live file alone would hand out a number an archived row already owns. `sync-backlog-rows.sh` also learned the difference between "archived on main" and "does not exist on main", which were previously the same misleading message. **`status/backend.md`: 128 KB → 121 KB only.** Its sections older than 30 days are a single one (#49, 2026-08-14); everything else genuinely falls inside the window this row specifies. The BACKLOG was the 7× win; backend.md is not one yet and will become one on its own schedule. — Original spec follows. **Archive `done` backlog rows so every session stops carrying 321 KB. (Audit 2026-09-09.)** `status/BACKLOG.md` is **321 KB**: 123 `done`/`dropped` rows = 196 KB, plus 52 KB of prose after the table (spend gates, FX anchors, #9/#10 notes — 2026-08 history). Lanes only *grep* it, but every interactive session and every lane that proceeds past the gate reads it. **Fix:** (a) move every `done`/`dropped` row whose phase is < the lowest open phase (today: everything ≤ phase 6, and any `done` row no open row `needs`) to `status/archive/BACKLOG-done.md`, same table format, unchanged text; (b) teach `status/check-backlog.sh` to read **both** files for the duplicate-number check and for `needs` resolution (a `needs` pointing at an archived row is valid); (c) move the post-table prose into `status/archive/BACKLOG-notes-2026-08.md` and leave a one-line pointer; (d) `status/README.md` numbering rule: max+1 over **both** files. Target < 120 KB. Do the same for `status/backend.md` (110 KB): move `## Done` entries older than 30 days into `status/archive/backend-history.md` (it already exists). Safe: `status/**` matches no build/deploy filter. |
| 194 | backend | 9 | done | 187 | **DONE 2026-09-13 (Radu said "do 194 now") — path (b), backend-backed, shipped to `main`; extension at 1.5.0.** Migration `037_history.sql` (`history (token_hash, url_key, saved_at, payload jsonb)`, PK `(token_hash, url_key)`, `(token_hash, saved_at DESC)` index; token stored as sha256, never plaintext). New `routes/history.js`: `GET /api/history` (this token's live rows, newest first), `POST /api/history` (upsert one entry by server-derived `url_key`, server stamps `saved_at`, prunes on write), `DELETE /api/history` (the popup Clear button) — all behind `INGEST_TOKEN` on both verbs so a read and a write from the same token hit the same rows. Pure `src/lib/history.js` (`historyKey` + `RETENTION_DAYS`/`MAX_ENTRIES`), with `history-lib.test.js` pinning those constants and `historyKey` to the extension's copy so the two can't drift. `extension/history.js` `loadHistory`/`recordVisit`/`clearHistory` are now remote-first with `chrome.storage.local` as an offline cache (dynamic-imports `settings.js` to keep the unit-tested pure half import-free); no write token → old local-only behaviour. 467/467 backend tests green (17 new). iOS surfacing stays #195. **Original ask (Radu, 2026-09-11: verbatim, "Sync shortlist between browsers using extension").** The shortlist is #187's Top-coffees list, currently in `chrome.storage.local` — per-browser only, so Chrome-at-home and Brave-at-work see nothing of each other, and it never reaches iOS. #187 flagged this exit explicitly: *"a backend-backed version is a separate row if Radu wants one."* **Two paths, default = the backend one:** **(a)** swap `chrome.storage.local` → `chrome.storage.sync` (one-line change per site) — Google-account-scoped, works between Chromes signed into the same account, dies at 100 KB total / 8 KB per item and does not reach Firefox/Brave/iOS. **(b)** move the list to a new `/api/history` endpoint keyed by the token the extension already holds (`INGEST_TOKEN`), so any browser he types the token into sees the same shortlist and the same rows are readable from iOS/website later — this is the default. Sketch: `GET /api/history` returns the pruned + ranked list (server does the retention math once, not per client); `POST /api/history` upserts one entry by origin+path key, same shape `history.js` already writes; pruning runs server-side on write per #187's reasoning (a client that has not opened in 10 days must not un-prune a row on next sync). Migration: one `history` table `(token_hash, url_key, saved_at, payload jsonb)`, PK `(token_hash, url_key)`; token stored hashed so the DB never carries the bearer plaintext. Client keeps a small local cache for offline reads and races the network on popup open, mirroring how `getConfig`/`getScore` already behave. Extension bumps to 1.5.0; `history.js` gains a remote-first `loadHistory`/`recordVisit` and the pure functions stay pure. **Not part of this row:** any UI change (rows still render the same), any iOS surfacing (separate row once this lands), any scoring change. **Open, for Radu:** the `chrome.storage.sync` route is one commit and zero backend work if he only cares about Chrome-to-Chrome and wants nothing on iOS ever; say the word and the row narrows. |
| 195 | ios-ux | 10 | done | 155, 157, 194 | **DONE 2026-09-14 (ios-ux), `ios-staging`.** Nav bar is **Coffees · Recipes · + · Shop · Insights**, verbatim per Radu — five items, still under the count at which iOS folds the last tab into a "More" list, and the `+` keeps its tab-bounce. **(a) Recipes** wraps `BrewCatalogueView` (#157), which already existed but was reachable only from Settings — discovery was the problem, not the path, so it stays in Settings too. **(b) Shop** renders `GET /api/history` **as the server orders it**: the extension already ranks and prunes (#196's manual adjustment, #197's 30-day window from *add* time, #199's re-blended score), and a second ranking on the phone would be a second definition of one list — the two surfaces would disagree about which bag is top with no way to tell which was right. Rows mirror `CoffeeRowView`'s layout, flag `IN YOUR LIBRARY` on a roaster match, and open the shop page in Safari. **Seam edits** (recorded in both lane files): `RootTab` gains `.recipes`/`.shop`; `CoffeeStore` gains `shortlist`/`shortlistError`/`loadShortlist()`; `APIClient` gains `shortlist()`; new `Models/Shortlist.swift` + a wire DTO. `ShortlistEntry` decodes every field as optional except the url, because the server stores the extension's payload **opaquely** and the two ship on different schedules — CLAUDE.md §12's public-contract rule from the other side — with `FailableDecodable` per row so one odd entry cannot empty the tab. **⚠ Open for Radu:** both new tabs use **SF Symbols, not Lucide** (Redesign v3 §11). `Assets.xcassets` has 13 imagesets and none is a flask or a storefront; adding one means art, which is his call. Named `tabRecipesFallback`/`tabShopFallback` in `Symbols.swift` so the placeholder cannot be mistaken for a decision. — Original spec follows. **Nav bar: add Shop + Recipes tabs, reorder to Coffees · Recipes · + · Shop · Insights (Radu, 2026-09-12: verbatim, *'add 2 new sections: Shop (presenting the shortlist from the extension) and Recipes (the new speced section with recipes, devices, grind size and asasociations with coffees). order: Coffees, Recipes, +, Shop, Insights'*).** Today's `RootTabView` (`Sources/Features/Root/RootTabView.swift`) has three tabs: Coffees · + · Insights (Redesign v3 §3, the plain `TabView` after the v2 custom bar was reverted). **(a) Recipes tab** — a landing surface for the Brew lab feature (#155-#158 shipped `ios-staging`, backend live) that today only lives inside a coffee's detail page. Not a list of past logs — a **catalogue view** of the vocab (recipes, devices, grind clicks, temperatures) with the per-option 'won N of M' from `CoffeeIndex.brewWinRates(kind:)`, tap → Coffees filtered to that option via `selectInCoffees(dimension:key:)` (same deep-link #158's Insights card already uses). Reuse `Features/Brew/BrewCatalogueView` (which today opens from Settings) as the tab root, or a new `RecipesTabView` wrapping it — decide when building. **(b) Shop tab** — surfaces the extension's shortlist (#187 → #194). Depends on #194 shipping the server-side `/api/history` endpoint, since the iOS app has no way to read `chrome.storage.local`. Row shape mirrors `CoffeeRowView` (the extension popup already does this — reuse the same field layout), tap opens the roaster page in Safari via the saved `url`. Owned coffees flagged 'IN YOUR LIBRARY' (extension does this via #161's match; the same match runs in iOS from the same corpus). **(c) Order + centre `+`** — Coffees · Recipes · + · Shop · Insights, with `+` still the tab-bounce trick that opens `AddCoffeeWizardView` and restores the previous tab (existing `.onChange` in `RootTabView`). The tab bar goes from 3 items to 5; verify the native glass bar still reads right at 5 items (it does — that's still comfortably under the 5-item limit before iOS turns the last one into 'More'). **Blocked on #194** (Shop tab has nothing to show without the server endpoint) and cross-lane on ios-shell for a `CoffeeStore.shortlist` accessor + fetch that mirrors how `snapshot`/`review` load — file that when #194's shape lands. Recipes tab is not blocked on shell (#156 already published `CoffeeIndex.brewWinRates` and `Vocabulary.brewOptions`) but ships in the same row for a single nav-bar change. **Not part of this row:** any change to the Brew lab feature itself (#155-#158's design stands), any change to the extension's popup Top-coffees list (that stays where it is, so the extension is still useful without the iOS app), any Shop-tab filtering/sorting beyond the extension's own ranking. Sync the row flip to `main`. |
| 200 | ios-ux | 10 | done | — | **App Settings → What's New: add per-entry seen state, unseen badge, Mark all seen (Radu, 2026-09-12: *'@brief for extension - implement now. add new feature under settings - what's new, brief attached'* then correction *'whatsnew - i want it in the app under settings!!!'*; brief `cf07c834-whats-new-feature-brief.md`).** Original ask was implemented against the browser extension and reverted the same session (`ea03463`, extension back at 1.4.1) after Radu clarified. The app already has `ios/MyCoffee/Sources/Features/WhatsNew/WhatsNewView.swift` (Live/Plan segments, read-only, reachable from `SettingsSheet`); this row adds the brief's checked-state behaviour. **Shape:** new `WhatsNewSeenStore` (singleton per the brief's *one shared store* rule; `Set<String>` of stable keys backed by `UserDefaults`), key = FNV-1a hash of `title\ndetail` so a rewrite keeps its check and a genuine retitle legitimately unchecks. `WhatsNewView` gets a leading checkbox per card, a **Mark all seen** toolbar button, and a live count in the segmented picker (`Live · 3 new`). `SettingsSheet` puts a `.badge(unseenLive)` on the nav link so the count is visible before opening it. **Live entries only for the badge** — Plan items are things Radu already asked for; badging them would fire the moment a row is filed. **Seen state stays on-device for now.** The brief's `GET/POST /api/whatsnew/seen` endpoints don't exist yet; add them (+ `whatsnew_seen` table) only when a second device (iPad, second phone) genuinely needs to sync — filed as a separate follow-up rather than pulled into this row, since MyCoffee has no per-user identity today (auth is one shared `INGEST_TOKEN`, so "sync across devices" is really "share Radu's phone with Radu's iPad," small and later). **Files:** new `Features/WhatsNew/WhatsNewSeenStore.swift`; modify `Features/WhatsNew/WhatsNewView.swift` and `Features/Coffees/SettingsSheet.swift`. All ios-ux; no shell seam. **Sync the row flip to `main`** via `status/sync-backlog-rows.sh 200`. |
| 201 | ios-ux | 10 | done | 200 | **DONE 2026-09-14 (ios-ux), `ios-staging`.** iOS half shipped. `WhatsNewSeenStore.startSync(config:)` hydrates from `GET /api/whatsnew/seen` and **REPLACES** the local set — a union could never un-check anything, so un-ticking on the iPad would be silently undone by the phone's next sync; last-writer-wins per key is the brief's rule and the server is the writer of record. `toggle`/`markSeen`/`markNotSeen` write through to `POST /api/whatsnew/seen` fire-and-forget; a failed POST stays local and the next successful GET reconciles. UserDefaults stays the first read, so checkmarks are right the instant the sheet opens and the server only ever corrects them. Called from **both** `WhatsNewView` and `SettingsSheet` — without the second, the badge would count entries already ticked on the iPad as new until you opened the sheet, which is the one moment a badge exists to save you. **Seam edit** (CLAUDE.md §4): `APIClient.whatsNewSeen()`/`setWhatsNewSeen(key:seen:)` + two DTOs added to ios-shell-owned `API/**` — two thin wrappers over the existing `send`/`makeRequest`, no new shell logic, recorded in both lane files. — Original spec follows. **Cross-device sync for What's New seen state, phone ↔ iPad (Radu, 2026-09-12: verbatim, *'Well I want exactly iPhone iPad sync for what's new that's the whole purpose'*).** Radu redirected 2026-09-12: *'Don't build now let it be picked by lanes'* — backend half is already live on `main`; iOS half waits for the next ios-ux firing. **Backend half — SHIPPED `main@e96ea43` and live on Railway.** Migration 036 (`whatsnew_seen (entry_key PK, seen_at)` — no user_id, since MyCoffee has one shared INGEST_TOKEN and therefore one seen set); `routes/whatsnew.js` extended with `GET /api/whatsnew/seen` (requireAnyToken, `{seen: [key…]}`) and `POST /api/whatsnew/seen` (requireIngestToken, `{key, seen?}`, seen defaults true; ON CONFLICT idempotent; 4 KB key cap; returns the fresh set). Server treats keys as opaque strings — the FNV-1a hash of title+detail is a client-side convention #200's `WhatsNewSeenStore` already computes. 450/450 tests green (8 new). Endpoints are inert until iOS calls them. **iOS half — for the ios-ux lane to pick up:** extend `Features/WhatsNew/WhatsNewSeenStore.swift` — hydrate from `UserDefaults` first (unchanged: never blocks render), add `startSync(config:)` that fires `GET /api/whatsnew/seen` and **replaces** the local set on success (brief's *last-writer-wins per key*), extend `toggle`/`markSeen` to accept a config and fire `POST /api/whatsnew/seen` after the local write (a failed POST logs and stays local; the next successful GET reconciles). `Sources/API/APIClient.swift` gets `whatsNewSeen()` and `setWhatsNewSeen(key:seen:)`. `WhatsNewView` calls `seenStore.startSync(config: config)` in a `.task` alongside `load`; `SettingsSheet` does the same in its own `.task` so the badge count reflects the iPad's marks even before you open the sheet. **Seam edit needed** (CLAUDE.md §4): `APIClient.swift` is ios-shell-owned. ios-ux adds the two minimal method arms in the same commit as a plain add and records it under `## Claimed` in both lane files, per the seam rule. No new types, no shell logic — just two thin wrappers around existing `send`/`makeRequest`. **Not part of this row:** any auth-per-device model (there is no per-user identity today; one shared token is by design), the brief's `not-done` feedback loop (separate row if Radu wants it), and any UI change (#200's checkboxes + badge already read the store — nothing to touch). Sync the row flip to `main`. |
| 205 | ios-ux | 10 | done | — | **DONE 2026-09-14 (ios-ux), `ios-staging` — compile-green run #135.** **(a)** segment is now **Live · Done · Plan**. `Done` is the *checked complement* of Live, derived from `WhatsNewSeenStore` rather than a fourth backend section: `/api/whatsnew` has `live` and `plan` and nothing else, and "done" is a per-person fact the server has no opinion about. Live and Done **partition** `response.live`, so nothing can hide in a gap between them; the `Live · N new` count affordance is kept. **(b)** a second segmented row **All · ✓ · ☐** filtering the current segment by seen state — hidden on Done, where every card is checked by construction and a filter whose only non-empty option is "checked" would be a dead control. **(c)** trailing swipe **"Not done"** (the screenshot's orange flag) routed through the same store via a new **idempotent** `markNotSeen` — swiping an already-unchecked card is a no-op, which `toggle()` would have turned into a re-check. One definition of "done" is the entire reason #200 built a store rather than a per-view flag. **(d)** confirmed: cross-device sync is #201, shipped in the same session. — Original spec follows. **What's New was not built to the full brief — add the Done segment, the seen-state filter row, and swipe-to-mark-not-done (Radu, 2026-09-13: *"whats new was not implemented as speced… it does not show live/done/plan, it does not show all / checked / unchecked, it does not have swipe / mark as not done"*, with a reference screenshot).** #200 shipped the per-entry checkbox, Mark-all-seen and the Settings badge, but against the current two-segment `WhatsNewView` (Live / Plan only) — it did not implement the rest of the brief `cf07c834-whats-new-feature-brief.md`, whose reference is in Radu's 2026-09-13 screenshot. **The brief file is NOT in the repo (it was an attachment to #200); transcribe from the screenshot, which is the durable spec here.** Gaps to close, all in `ios/MyCoffee/Sources/Features/WhatsNew/WhatsNewView.swift` (and its `Segment` enum at ~`:9`): **(a) Top segment becomes Live · Done · Plan** — a third `Done` case beside the existing `.live`/`.plan` (the screenshot's top row). Decide what `Done` lists — most likely the checked-off Live entries — from the brief's intent; keep the `Live · N new` count affordance. **(b) A second segmented row All · ✓ · ☐** filtering the current segment's cards by seen-state (All / checked / unchecked), matching the screenshot's second control row. **(c) Swipe action "Not done"** on a card to *un*-check it (the screenshot's orange trailing-swipe flag), the inverse of tapping the checkbox — wire it through the existing `WhatsNewSeenStore.toggle`/`markSeen` so it stays one store. No new seen-state model is needed (#200's `WhatsNewSeenStore` already holds it); this is presentation + a swipe action over that store. **(d) Cross-device sync (iPad ↔ iPhone) is NOT part of this row — it is #201** (backend half live on `main`, iOS half `ready` for the ios-ux lane); Radu's "does not sync" is that #201's iOS half hasn't been picked up yet, not a defect in #200. All ios-ux, no shell seam. Sync the row flip to `main`. |
| 206 | backend | 10 | ready | — | **Safari Web Extension port of the MyCoffee evaluator (Radu, 2026-09-13: verbatim, *"also do a safari extension, add to plan"*).** Today `extension/**` is a Chrome MV3 web extension (backend-lane-owned, §4). Safari 16+ runs MV3 web extensions, but unlike Chrome it cannot load an unpacked folder: a Safari extension must be wrapped in an **Xcode Safari Web Extension app** (a container app + an extension appex), signed under an Apple team, and distributed through the App Store (iOS) or App Store / Developer-ID-notarized (macOS). **Most of the code ports as-is** — `background.js`, `popup.*`, `scrape.js`, `history.js`, `settings.js` are portable, and #194 already made the shortlist backend-backed, so a Safari copy shares Radu's shortlist the moment he enters the token (no per-browser data to migrate). **Safari-specific work:** (a) generate the wrapper with `xcrun safari-web-extension-converter extension/` into a new top-level project (e.g. `safari/`), keeping `extension/` as the single source so Chrome and Safari build from one codebase (a build/copy step, not a fork); (b) **signing** — a new App ID / bundle id for the container app + `.appex` under team `PH2NNQ47UB`, provisioned via `match` (publish lane, private repo) — a manual Apple-portal step like the main app's App ID and #154's APNs key, to be recorded in `BUILD_STATUS.md`; (c) **auto-update** — the GitHub-raw `update.js` mechanism is Chrome-unpacked-only and must be **no-op'd on Safari** (the App Store handles updates there), gated by runtime detection so the shared code still works in Chrome; (d) **API audit** — the code uses `chrome.*` (Safari supports it, and `chrome.scripting`/`chrome.storage.local`/`chrome.alarms`/`chrome.tabs` all exist in Safari 16+), but confirm no Chrome-only call and that the service-worker fetch stays CORS-exempt under Safari's per-site host-permission prompt; (e) a build/sign **workflow** analogous to `ios-testflight.yml` (macOS runners are free). **Decisions for Radu — DON'T pick silently:** (1) **target** — iOS Safari (lets him shortlist on iPhone/iPad with no Chrome option — the biggest win, and it complements the Shop tab #195), macOS Safari, or both; recommend **both from one converter project**, iOS-first if only one. (2) **distribution** — App Store (required for iOS; cleanest auto-update on macOS too) vs macOS Developer-ID notarized (side-loadable, no review); recommend App Store. (3) confirm the **single-source** approach in (a) over a forked copy (recommend single source). **Cross-lane + manual:** portable code + converter scaffold + `update.js` gating are backend-lane and can start now; the Xcode wrapper, `match`/entitlements and the ship workflow are publish-lane + the manual Apple App-ID step — split those into follow-up rows (like #154) once Radu picks target + channel. Bump `extension/manifest.json` only if the shared manifest changes for Safari compat. `extension/**`, new `safari/**`, `.github/workflows/**` (publish), `match` (private repo). |
| 207 | data | 10 | human | — | **⚠ 2026-09-14 — status set to `human`: the code half is DONE, what remains is a decision only Radu can make.** Set to `human` per `status/README.md` ("when a task turns out to need a human, set its status to `human` so no lane claims it") — leaving it `ready` just means the next data-lane firing re-reads it and asks the same two questions again. **Shipped:** (1) migration `038` backfilled the 49 staged blurbs (15 → 64/110); (2) the checklist generator now tracks **delivery, not staging**, on BOTH columns — the Blurb column was already fixed to read the live `blurb`, and the **Logo column now reads the snapshot's `logoUrl` too** (2026-09-14), with a third state `◐` for "art staged in `logos/` but no migration has wired it into `roasters.logo_url`", which is a different job from `☐` "nobody has found a logo yet". Regenerated against production: **all 64 in-library roasters are complete — logo AND blurb both live in the DB, 0 still need content, and 0 logos are staged-but-unwired.** The entire remaining gap is the 46 **zero-coffee** seeded roasters, none of which has either. **Two questions for Radu, both blocking:** (1) **Is the 46-roaster tail worth doing at all?** They have no coffees, so they surface only in vocab pickers — the checklist has always called them low priority, and one (`zzz-diagnostic-roaster`) is a test fixture that must never get content. If yes: agent-researched (recommended — he's flagging gaps, not pasting 46) or he pastes them. (2) **Sophia** — still not in the roaster vocab under that name, and no coffee references it. Give the full roaster name, or confirm it's new and should be added. Don't guess. — Original spec follows. **Backfill the missing roaster blurbs — 95 of 110 roasters have none (Radu, 2026-09-13: *"Sophia has no blurb"*, then *"Same for man vs machine"*).** #133 populated blurbs from the content Radu pasted at the time, but a live `/api/snapshot` count (2026-09-13) shows only **15/110 roasters carry a blurb; 95 are null** (46 also have no logo — a separate track, the logo intake sweep is paused per CLAUDE.md §10). **⚠ 2026-09-14 correction (Radu: the checklist says all blurbs provided — why does the app show 95 missing?):** most of this is NOT research. **49 of the 95 missing blurbs are ALREADY written and staged in `ops/roaster-assets/blurbs.md`** (all 64 in-library roasters show ✅ in `CHECKLIST.md`) — but ✅ there means *staged in the markdown file*, NOT *written to the DB*. Only migrations `029` (13) + `030` (2) = **15 roasters** were ever applied; no migration `034+` was authored for the rest, so the staged text never reached the backend the app reads. **So the fast win: an idempotent, slug-keyed migration that applies the 49 already-staged blurbs from `blurbs.md` — no sourcing, no fabrication, they're Radu-approved text.** Only the remaining ~46 (the "0 coffees / seeded-but-unused" roasters, already low priority) have no staged text and would need sourcing. **Also fix `gen-checklist.py` so the ✅ Blurb column reflects the live `/api/snapshot` `blurb` column, not the presence of a `blurbs.md` heading** — otherwise the checklist keeps reporting green for content that was never delivered (this is the exact trap that hid the gap). **✅ 2026-09-14 — the 49 staged blurbs SHIPPED (migration `038_roaster_blurbs_backfill.sql`, Radu-approved in an interactive session; verified live 15→64/110 with blurbs).** All 64 in-library roasters now carry a blurb in the DB. **Remaining under this row:** (1) the **46 zero-coffee "seeded-but-unused" roasters** that have no staged text — the sourcing tail; low priority per the CHECKLIST, and one (`zzz-diagnostic-roaster`) is a test fixture that should never get one; (2) the **`gen-checklist.py` fix** above (still reads `blurbs.md`, not the DB). Do NOT re-run a migration for the 49 — `038` is idempotent (`blurb IS NULL` guard) and already applied. The read path and column already exist (`roasters.blurb`, surfaced via #132/#134), so this is pure content. **Radu's two examples:** *Man vs Machine* (id 49, slug `man-vs-machine`) is in the vocab with a logo and a null blurb — confirmed. ***Sophia is NOT in the roaster vocab at all*** (no roaster or farm by that name, and no coffee references it) — so it's either a new roaster to add or a name mismatch; **DON'T guess — the exact name/handling is a question for Radu** (see the note below). **Approach:** source a short factual blurb per roaster the same way #38 (countries) and #133 (logos) were done — web-searched, one at a time, verified, no fabrication — matching the voice of the existing 15 (e.g. Gardelli, Tim Wendelboe, April: 2–4 sentence origin/ethos prose). Write via an **idempotent migration keyed on `slug`** that only sets `blurb` where it IS NULL (never overwrites a human/edited value; `IS DISTINCT FROM` + bump `updated_at` so the iOS delta sync ships it). Keep/refresh the #133 "still missing" checklist so progress is visible; batch is fine (the corpus is ~95). **Decision for Radu — DON'T pick silently:** (1) **who writes them** — agent-researched blurbs (recommended: he's flagging gaps, not pasting 95, and the existing 15 read as researched) vs Radu pastes each; either way #153 (the in-app editor, blocked on #152) is the ongoing self-serve so he can correct any he dislikes. (2) **Sophia** — give the full roaster name (or confirm it's new and should be added to the vocab first, via the #36 get-or-create path). `backend/migrations/0NN_roaster_blurbs*.sql` (data-owned per §4), no schema change. |
| 210 | human | 10 | human | — | **Lane cadence is now above what the queue needs — decide whether to revert (Radu's own rule, #183).** His rule: raise a lane's cadence while more than 5 of its rows are `ready`, drop back when the queue is under 5. After the 2026-09-14 sweep the counts are **backend 4 ready, ios-shell 0, ios-ux 0, data 0** — all four backend rows (#126, #127, #152, #206) are themselves waiting on a decision from him, so the *effective* queue is 0. By the letter of the rule: backend `23 7 * * 1,3,5` → `23 7 * * 1,4`, and both iOS lanes `* * 1,2,3,4,5,6` → `* * 1,3,5`. **Left as `human` rather than done, because the right answer depends on his answers:** #126, #152 and #206 are each a decision that unblocks 1–3 rows (#127→#128, #153→#154), so answering them refills the queue within a day and a revert now would be churn. Cron lives in the CCR routines, not the repo — `trig_01NRdGKCx6C4yZA8uKKPyT4Q` (backend), `trig_019bGG7fJ59PbC4tUBMaMZJs` (ios shell), `trig_01GVd3Hc2r2mX76TFPgo4odL` (ios ux). |
| 211 | ios-shell | 10 | ready | — | **Widen `Coffee.purchasedOn` to optional — the half of #178(b) that is a cross-lane change.** #178(b) made `CompactCoffeeDTO.purchasedOn` optional and turned the drop into a *deliberate, counted* one (`SnapshotDecodeStats` → `CoffeeStore.droppedRowCount`, surfaced by #192), so a coffee with no purchase date no longer vanishes silently. It still does not appear, though: `Coffee.purchasedOn` is non-optional, so `CompactCoffeeDTO.makeCoffee` returns nil for it. Widening the model ripples into **~15 UX-owned call sites** — `CoffeeIndex`'s canonical `purchasedOn`-desc sort and its relative-window filter, `SortOption`'s two comparators and both month-header formatters, `CoffeeDetailView`'s Purchased fact row, the `.year` facet, and three `InsightsView`/`InsightsFindings` aggregations — each of which needs a real decision about where an undated bag sorts and whether it belongs in a year bucket at all. **Measured 0 of 414 live rows affected today**, so this is latent, not urgent; the wizard's `quick-create` path (a photo with no `captured_at`) is how it starts. Seam rule applies: shell widens the model and adds the minimal compiling arms, UX decides the real placement in its own row. `Sources/Models/Coffee.swift`, `API/Wire/CoffeeMapping.swift`, then the UX sites. |
| 212 | ios-ux | 10 | ready | 178 | **Move the six UX-owned `APIClient(config:)` constructions onto #178(e)'s `CoffeeStore.makeAPIClient()`.** #178(e) added the single shell entry point and migrated the two shell-owned sites; the UX ones were left alone on purpose, because changing them in the same commit would have been a cross-lane edit for no behavioural gain (seam rule, CLAUDE.md §4). Remaining: `Features/Coffees/SettingsSheet.swift:68,82`, `Features/Review/CoffeeReviewSheet.swift:59`, `Features/Review/ReviewQueueView.swift:128`, `Features/Review/ReviewFeedCache.swift:55`, `Features/WhatsNew/WhatsNewView.swift:86`. **`ConnectView.swift` is the one to leave**: it builds a client from a config the user is still typing, before `AppConfig.shared` is valid, which is exactly the case `makeAPIClient()` cannot serve. Purely mechanical otherwise — the point is that a future change to how a client is configured (a header, a timeout, a base-URL fallback) has one place to remember instead of eight. |

---

**Older rows and notes are archived**, so every session stops carrying them
(#182):

- **`status/archive/BACKLOG-done.md`** — completed rows below the lowest open
  phase. Row numbers are never reused; `check-backlog.sh` reads both files for
  the duplicate check and for `needs`, and `status/README.md`'s "max + 1" rule
  means max over **both**.
- **`status/archive/BACKLOG-notes-2026-08.md`** — the 2026-08 spend gates, FX
  anchors and #9/#10 write-ups that used to sit under this table.
