git_url(ENV["MATCH_GIT_URL"])      # set by CI to Climb-Again/mycoffee-private
git_branch("match")                # orphan branch there; match rewrites README.md
storage_mode("git")
type("appstore")
app_identifier(["ro.climbagain.mycoffee"])
