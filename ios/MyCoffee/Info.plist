<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>MyCoffee</string>
	<key>CFBundleName</key>
	<string>MyCoffee</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- #103: REQUIRED. iOS terminates the app the instant it touches the
	     camera without this key — a hard kill, not a permission denial.
	     The Add Coffee wizard offers a camera capture alongside the photo
	     library. No NSPhotoLibraryUsageDescription is needed: PhotosPicker
	     uses PHPickerViewController, which runs out of process. -->
	<key>NSCameraUsageDescription</key>
	<string>MyCoffee uses the camera to photograph a coffee bag when you add it, so the printed details can be read from the label.</string>
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>UILaunchScreen</key>
	<dict/>
	<!-- #114: the app was portrait-only on every device, so no landscape
	     layout could ever be seen. iPhone gains both landscapes (but not
	     upside-down, which no iPhone app wants); iPad declares all four,
	     which is what iPadOS requires of an app that does not opt out of
	     multitasking with UIRequiresFullScreen. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<!-- #175(e): no BGTaskScheduler task is registered anywhere in the app —
	     `ro.climbagain.mycoffee.refresh` was declared here and mentioned only
	     in comments, never wired to a handler. Removed rather than built: the
	     30 MB image-cache eviction already runs cheaply at every launch
	     (`RootView`/`ImageStore.evictStaleEntries`), so there's nothing this
	     app currently needs woken up in the background to do. -->
</dict>
</plist>
